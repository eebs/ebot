<% if var(:unauthorized) %>
    You are unauthorized to perform this action!
<% elsif var(:failed) %>
    No lunch suggestions.
<% else %>
    <%= var(:lunch).creator.name %> <%= 'suggested' %> <%= var(:lunch).lunch %>.
<% end %>
